# Root Environment Variables
# This is a template file for environment variables
# Copy this to .env and fill in your values

# Note: Backend-specific environment variables are in packages/backend/.env
# Note: Frontend-specific environment variables should be prefixed with VITE_

# Example frontend variables (if needed):
# VITE_API_URL=http://localhost:4000/graphql
# VITE_APP_NAME=Bookstore App
